{
  "name": "laundry-app",
  "version": "1.0.0",
  "private": true,
  "description": "Root helper scripts. Dependency yang sebenarnya ada di backend/package.json — jangan duplikasi versi di sini.",
  "scripts": {
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev",
    "install:backend": "npm --prefix backend install",
    "vercel-build": "echo 'Build complete'"
  }
}
